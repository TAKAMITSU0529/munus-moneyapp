{
  "buildCommand": "npx expo export && node scripts/inject-ogp.mjs && cp -r public/* dist/",
  "outputDirectory": "dist",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}